#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../firebase-tools/bin/firebase" "$@"
  ret=$?
else 
  node  "$basedir/../firebase-tools/bin/firebase" "$@"
  ret=$?
fi
exit $ret
